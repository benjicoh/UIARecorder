<Window x:Class="Recorder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Recorder"
        xmlns:viewModels="clr-namespace:Recorder.ViewModels"
        xmlns:utils="clr-namespace:Recorder.Utils"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        Style="{DynamicResource MaterialDesignWindow}"
        Foreground="White"
        mc:Ignorable="d"
        Title="Recorder"
        SizeToContent="Height"
        MinWidth="1000"
        MinHeight="500"
        Width="1200">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <utils:LogLevelToBrushConverter x:Key="LogLevelToBrushConverter" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <tb:TaskbarIcon
            x:Name="TaskbarIcon"
            IconSource="/Resources/recorder.ico"
            ToolTipText="{Binding TrayToolTipText}">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Show/Hide" Command="{Binding ShowWindowCommand}" />
                    <MenuItem Header="Exit" Command="{Binding ExitApplicationCommand}" />
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>

        
        <StackPanel Grid.Row="0" Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="10">
            <TextBlock Margin="10"
                       FontSize="18"
                       Text="Record"
                       VerticalAlignment="Center"/>
            <ToggleButton Width="64"
                          Height="32"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Center"
                          Style="{StaticResource MaterialDesignSwitchDarkToggleButton}"
                          IsChecked="{Binding IsRecording}"
                          Command="{Binding ToggleRecordingCommand}"
                          materialDesign:ButtonProgressAssist.IsIndeterminate="False">
            </ToggleButton>
            <TextBlock Margin="30,10,10,10"
                       FontSize="18"
                       Text="Capture Mode"
                       VerticalAlignment="Center"/>
            <ComboBox Width="150"
                      ItemsSource="{Binding CaptureModes}"
                      SelectedItem="{Binding SelectedCaptureMode}"
                      VerticalAlignment="Center"/>

            <Button Content="Select Area"
                    Command="{Binding SelectCaptureAreaCommand}"
                    Margin="10"
                    IsEnabled="{Binding IsNotRecording}"/>
            <TextBlock Text="{Binding CaptureAreaInfo}"
                       VerticalAlignment="Center"
                       Margin="10"/>

            <Button Content="Show Console"
                    Command="{Binding ShowConsoleCommand}"
                    Margin="10" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <GroupBox Header="Whitelisted Processes" Grid.Column="0" Margin="5">
                <DockPanel>
                    <Grid DockPanel.Dock="Bottom" Margin="5,0,5,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding NewProcessName, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="New Process Name" Style="{StaticResource MaterialDesignFloatingHintTextBox}" VerticalAlignment="Center"/>
                        <Button Grid.Column="1" Content="Add" Command="{Binding AddProcessToWhitelistCommand}" Margin="5,0,0,0"/>
                        <Button Grid.Column="2" Content="Remove" Command="{Binding RemoveProcessFromWhitelistCommand}" Margin="5,0,0,0"/>
                    </Grid>
                    <ListView x:Name="WhitelistListView" ItemsSource="{Binding WhitelistedProcesses}" SelectedItem="{Binding SelectedProcess}" Margin="5" MinHeight="100" />
                </DockPanel>
            </GroupBox>

            <StackPanel Grid.Column="1" Margin="5">
                <GroupBox Header="Project Directory" Margin="0,0,0,5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding ProjectDirectoryPath, FallbackValue='No project selected'}"
                                   VerticalAlignment="Center" Margin="5" TextTrimming="CharacterEllipsis" Grid.Column="0" />
                        <Button Content="Browse" Command="{Binding BrowseProjectDirectoryCommand}"
                                Grid.Column="1" Margin="5" IsEnabled="{Binding IsNotGenerating}" />
                    </Grid>
                </GroupBox>

                <GroupBox Header="Recording Directory" Margin="0,5,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding RecordingDirectoryPath, FallbackValue='Default recordings folder'}"
                                   VerticalAlignment="Center" Margin="5" TextTrimming="CharacterEllipsis" Grid.Column="0" />
                        <Button Content="Browse" Command="{Binding BrowseRecordingsDirectoryCommand}"
                                Grid.Column="1" Margin="5" IsEnabled="{Binding IsNotRecording}" />
                    </Grid>
                </GroupBox>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2">
            <Button Content="Generate Test"
                    Command="{Binding GenerateTestCommand}"
                    Margin="5"
                    IsEnabled="{Binding IsNotRecordingAndNotGenerating}" />

            <ProgressBar IsIndeterminate="{Binding IsGenerating}"
                         Margin="5"
                         Height="5"
                         Visibility="{Binding IsGenerating, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </StackPanel>


    </Grid>
</Window>
